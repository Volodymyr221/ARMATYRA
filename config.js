/* =========================================================
   КОНФІГ САЙТУ — КомпозитПром
   Винесені налаштування інтеграцій та контактів.
   =========================================================

   ⚠️  ВАЖЛИВО ПРО БЕЗПЕКУ TELEGRAM:
   Цей файл виконується у браузері, тому BOT_TOKEN буде видно
   будь-кому у вихідному коді сторінки. Для тесту / MVP це працює,
   але для продакшену токен краще тримати на сервері.

   Безпечний варіант (рекомендовано для продакшену):
   перенести надсилання у Netlify Function / Supabase Edge Function,
   а тут лишити лише ендпоінт. Деталі — у README.md.
   ========================================================= */

window.SITE_CONFIG = {
  // Контактний телефон (використовується у кнопках tel:)
  phone: "+380501234567",
  phoneDisplay: "+38 (050) 123-45-67",

  // ---- Telegram ----
  telegram: {
    // Увімкнути/вимкнути надсилання у Telegram
    enabled: true,
    // Токен бота від @BotFather
    BOT_TOKEN: "PASTE_YOUR_BOT_TOKEN_HERE",
    // ID чату або каналу, куди надсилати заявки (отримати через @userinfobot)
    CHAT_ID: "PASTE_YOUR_CHAT_ID_HERE",
  },
};
